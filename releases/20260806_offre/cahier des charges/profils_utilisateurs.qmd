---
title: "Profils utilisateurs et cas d'usage"
format: html
date: "2026-08-06"
---

Les tableaux de cas d'usage par profil et la matrice de couverture présentés dans ce document sont **générés depuis `backlog.csv`** (source de vérité du projet, 99 User Stories). Les colonnes éditoriales « Cas d'usage » et « Question concrète » sont conservées via un mapping figé ; les colonnes « MVP » et « Phrase méthode agile » sont calculées depuis le backlog (US principale de chaque cas d'usage).

```{python}
#| label: setup-mapping

# Mapping éditorial conservé depuis profils_utilisateurs.md — décision MOA 2026-08-06
CAS_USAGE = {
    "CU-ADMIN-01": "Créer un compte utilisateur et lui attribuer un profil",
    "CU-ADMIN-02": "Configurer les rôles et les droits d'accès par profil",
    "CU-ADMIN-03": "Restreindre l'accès à certaines données sensibles selon le profil",
    "CU-ADMIN-04": "Consulter le journal des actions et détecter des anomalies",
    "CU-ADMIN-05": "Lancer un audit d'usage pour adapter la plateforme aux besoins",
    "CU-DATA-01": "Importer un nouveau jeu de données et le normaliser",
    "CU-DATA-02": "Détecter des valeurs aberrantes et qualifier la fiabilité",
    "CU-DATA-03": "Gérer un bassin versant et ses captages dans le référentiel",
    "CU-DATA-04": "Calculer un indicateur et versionner sa méthode",
    "CU-DATA-05": "Construire un indice composite et documenter les méthodes",
    "CU-EXPERT-01": "Conduire l'analyse multicritère (pondérations, scénarios, comparaisons, contributions)",
    "CU-EXPERT-02": "Consulter les graphiques temporels et alterner les vues",
    "CU-EXPERT-03": "Identifier des tendances et détecter des dépassements",
    "CU-EXPERT-04": "Consulter la fiche synthétique du BVAEP prioritaire",
    "CU-EXPERT-05": "Utiliser l'aide à l'interprétation avancée pour prioriser",
    "CU-DECISION-01": "Consulter un tableau de bord synthétique et une fiche territoire",
    "CU-DECISION-02": "Comparer deux bassins versants pour prioriser des investissements",
    "CU-DECISION-03": "Identifier une tendance critique et détecter un dépassement de seuil",
    "CU-DECISION-04": "Générer un rapport PDF pour un conseil municipal",
    "CU-DECISION-05": "Consulter l'aide à l'interprétation avancée pour éclairer une décision",
    "CU-PUBLIC-01": "Vérifier la qualité de l'eau près de chez soi via la carte",
    "CU-PUBLIC-02": "Rechercher ma commune ou mon captage et consulter les indicateurs",
    "CU-PUBLIC-03": "Consulter les couches thématiques et zoomer sur un territoire",
    "CU-PUBLIC-04": "Appliquer un preset pour voir les territoires les plus exposés",
    "CU-PUBLIC-05": "Trier la liste des indicateurs par distance pour trouver les plus proches",
}

# Mapping éditorial conservé depuis profils_utilisateurs.md — décision MOA 2026-08-06
QUESTION = {
    "CU-ADMIN-01": "Combien de comptes « Décideur métier » ont été créés depuis l'ouverture et dans quelle province se connectent-ils le plus ?",
    "CU-ADMIN-02": "Quel profil doit voir la couche PPE sans pouvoir exporter les données brutes associées ?",
    "CU-ADMIN-03": "Faut-il masquer la localisation exacte des captages sensibles (Koné, Pouembout) aux comptes publics ?",
    "CU-ADMIN-04": "Quelle connexion tardive ou quel export CSV massif doit déclencher un signalement ?",
    "CU-ADMIN-05": "Quel indicateur (BBR, ICPE, incendies cumulés) est le plus consulté sur le Grand Nouméa ?",
    "CU-DATA-01": "L'inventaire des captages de la province Nord contient-il des lignes rejetées et comment les corriger ?",
    "CU-DATA-02": "La complétude du BBR sur le BVAEP de la Dumbéa est-elle suffisante pour publier l'indicateur ?",
    "CU-DATA-03": "Le captage de la Coulée est-il rattaché au bon bassin versant ?",
    "CU-DATA-04": "La version 2 de l'indicateur BBR modifie-t-elle les valeurs publiées l'an passé ?",
    "CU-DATA-05": "Quel jeu de données source alimente l'indicateur « occupation du sol » au sein de l'indice composite ?",
    "CU-EXPERT-01": "Sachant que le BVAEP de la Dumbéa a un statut PPE absent, une surface agricole de 15 %, 3 ICPE en amont et une géologie ruisselante, quel poids attribuer à chaque indicateur pour que le classement de criticité reflète le risque réel de dégradation ?",
    "CU-EXPERT-02": "Entre 2021 et 2025, comment a évolué la surface cumulée d'incendies sur le BVAEP de la Dumbéa, et cette hausse est-elle corrélée aux surfaces d'érosion identifiées la même année ?",
    "CU-EXPERT-03": "Le BBR du captage de la Dumbéa est-il passé sous le seuil de déficit en période d'étiage sur les deux dernières années, indiquant une tension quantitative sur la ressource ?",
    "CU-EXPERT-04": "Quels indicateurs contribuent le plus au score de criticité final du BVAEP de la Dumbéa : est-ce le statut PPE, la densité d'ICPE, la surface d'incendies cumulés, ou la géologie ?",
    "CU-EXPERT-05": "Parmi les 5 captages prioritaires du Grand Nouméa, lequel cumule le plus de facteurs de risque aggravants : absence de PPE + BBR déficitaire + forte densité d'ICPE + superficie brûlée récurrente ?",
    "CU-DECISION-01": "Quel BVAEP du Grand Nouméa affiche le plus mauvais BBR ce trimestre ?",
    "CU-DECISION-02": "Entre le BVAEP de la Dumbéa et celui de la Coulée, lequel concentre le plus d'ICPE et d'incendies cumulés ?",
    "CU-DECISION-03": "Le BBR du captage de Pouembout est-il passé sous le seuil d'alerte en période d'étiage ?",
    "CU-DECISION-04": "Quel rapport PDF présenter au conseil municipal de Farino pour justifier un investissement de sécurisation ?",
    "CU-DECISION-05": "Pourquoi le BVAEP de La Foa est-il classé prioritaire : PPE absente, densité d'ICPE ou occupation du sol ?",
    "CU-PUBLIC-01": "Le captage qui alimente mon quartier à Nouméa a-t-il un BBR correct ?",
    "CU-PUBLIC-02": "Quels indicateurs sont disponibles pour ma commune de Koné ?",
    "CU-PUBLIC-03": "La couche « périmètres de protection » est-elle visible autour de mon captage ?",
    "CU-PUBLIC-04": "Quels sont les 10 captages les plus exposés en province Sud ?",
    "CU-PUBLIC-05": "Quels captages sont les plus proches de mon domicile à Païta ?",
}
```

## Profil 1 : Administrateur plateforme

**Description** — Agent technique (OEIL) responsable de l'exploitation, de la sécurité et de la maintenance de la plateforme HydroScope. Gère les comptes utilisateurs, les droits d'accès, les paramètres système et l'audit des usages.

**Objectifs** :

1. Sécuriser les accès et garantir la conformité RGPD
2. Maintenir la disponibilité et les performances du système
3. Paramétrer les profils utilisateurs et les droits d'accès
4. Assurer la traçabilité complète des opérations
5. Suivre les usages pour adapter la plateforme aux besoins

```{python}
#| label: tbl-admin
#| tbl-cap: "Cas d'usage du profil Administrateur plateforme (généré depuis backlog.csv)"
#| output: asis

import glob
import yaml
import pandas as pd

backlog = pd.read_csv("backlog.csv", sep=";")
backlog["MVP"] = backlog["MVP"].astype(str).str.strip().str.upper().eq("TRUE")
us_mvp = dict(zip(backlog["ID User Story"], backlog["MVP"]))
us_phrase = dict(zip(backlog["ID User Story"], backlog["Phrase méthode agile"]))

rows = []
for path in sorted(glob.glob("specifications/CU-*.qmd")):
    meta = yaml.safe_load(open(path, encoding="utf-8").read().split("---", 2)[1])
    if meta["profile"] != "Administrateur plateforme":
        continue
    main_us = meta["us"].split(",")[0].strip()
    if main_us not in us_mvp:
        print(f"[warning] US principale {main_us} introuvable dans backlog.csv (fiche {meta['uc']})")
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "—", "Phrase méthode agile": "—"})
    else:
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "O" if us_mvp[main_us] else "X", "Phrase méthode agile": us_phrase[main_us]})

print(pd.DataFrame(rows).to_markdown(index=False))
```

---

## Profil 2 : Administrateur expert data

**Description** — Administrateur des données responsable de l'alimentation, de la qualité, du catalogue, des calculs d'indicateurs et de la construction de l'aide à la décision. Gère les référentiels géographiques et méthodologiques.

**Objectifs** :

1. Alimenter la plateforme avec des données fiables et normalisées
2. Qualifier la qualité des données et détecter les anomalies
3. Construire et maintenir les référentiels géographiques et méthodologiques
4. Produire les indicateurs et les rendre accessibles aux autres profils
5. Construire l'aide à la décision (AMC, seuils, pondérations) et gérer les exports

```{python}
#| label: tbl-data
#| tbl-cap: "Cas d'usage du profil Administrateur expert data (généré depuis backlog.csv)"
#| output: asis

import glob
import yaml
import pandas as pd

backlog = pd.read_csv("backlog.csv", sep=";")
backlog["MVP"] = backlog["MVP"].astype(str).str.strip().str.upper().eq("TRUE")
us_mvp = dict(zip(backlog["ID User Story"], backlog["MVP"]))
us_phrase = dict(zip(backlog["ID User Story"], backlog["Phrase méthode agile"]))

rows = []
for path in sorted(glob.glob("specifications/CU-*.qmd")):
    meta = yaml.safe_load(open(path, encoding="utf-8").read().split("---", 2)[1])
    if meta["profile"] != "Administrateur expert data":
        continue
    main_us = meta["us"].split(",")[0].strip()
    if main_us not in us_mvp:
        print(f"[warning] US principale {main_us} introuvable dans backlog.csv (fiche {meta['uc']})")
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "—", "Phrase méthode agile": "—"})
    else:
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "O" if us_mvp[main_us] else "X", "Phrase méthode agile": us_phrase[main_us]})

print(pd.DataFrame(rows).to_markdown(index=False))
```

---

## Profil 3 : Expert métier eau potable

**Description** — Ingénieur/e spécialisé(e) en eau potable qui consulte les indicateurs et approfondit l'analyse (AMC, scénarios, comparaisons). N'effectue pas de gestion de données en back-end mais exploite les résultats pour son expertise métier.

**Objectifs** :

1. Conduire l'analyse multicritère pour évaluer des scénarios
2. Consulter les indicateurs de menace sur l'eau potable d'un territoire
3. Suivre les tendances et détecter les situations critiques
4. Produire des synthèses pour alimenter la décision
5. Alterner entre les vues graphiques pour une analyse multi-échelle

```{python}
#| label: tbl-expert
#| tbl-cap: "Cas d'usage du profil Expert métier eau potable (généré depuis backlog.csv)"
#| output: asis

import glob
import yaml
import pandas as pd

backlog = pd.read_csv("backlog.csv", sep=";")
backlog["MVP"] = backlog["MVP"].astype(str).str.strip().str.upper().eq("TRUE")
us_mvp = dict(zip(backlog["ID User Story"], backlog["MVP"]))
us_phrase = dict(zip(backlog["ID User Story"], backlog["Phrase méthode agile"]))

rows = []
for path in sorted(glob.glob("specifications/CU-*.qmd")):
    meta = yaml.safe_load(open(path, encoding="utf-8").read().split("---", 2)[1])
    if meta["profile"] != "Expert métier eau potable":
        continue
    main_us = meta["us"].split(",")[0].strip()
    if main_us not in us_mvp:
        print(f"[warning] US principale {main_us} introuvable dans backlog.csv (fiche {meta['uc']})")
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "—", "Phrase méthode agile": "—"})
    else:
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "O" if us_mvp[main_us] else "X", "Phrase méthode agile": us_phrase[main_us]})

print(pd.DataFrame(rows).to_markdown(index=False))
```

---

## Profil 4 : Décideur métier

**Description** — Élu, directeur de service ou président d'association disposant d'un pouvoir de décision métier. Utilise HydroScope pour piloter les orientations stratégiques et opérationnelles en matière de gestion de l'eau potable.


**Objectifs** :

1. Disposer d'une vue synthétique de la qualité de l'eau sur son territoire
2. Comparer des territoires pour arbitrer des investissements
3. Suivre les tendances et détecter les situations critiques
4. Produire des rapports formalisés pour les instances décisionnelles
5. Consulter l'aide à la décision pour éclairer ses choix

```{python}
#| label: tbl-decision
#| tbl-cap: "Cas d'usage du profil Décideur métier (généré depuis backlog.csv)"
#| output: asis

import glob
import yaml
import pandas as pd

backlog = pd.read_csv("backlog.csv", sep=";")
backlog["MVP"] = backlog["MVP"].astype(str).str.strip().str.upper().eq("TRUE")
us_mvp = dict(zip(backlog["ID User Story"], backlog["MVP"]))
us_phrase = dict(zip(backlog["ID User Story"], backlog["Phrase méthode agile"]))

rows = []
for path in sorted(glob.glob("specifications/CU-*.qmd")):
    meta = yaml.safe_load(open(path, encoding="utf-8").read().split("---", 2)[1])
    if meta["profile"] != "Décideur métier":
        continue
    main_us = meta["us"].split(",")[0].strip()
    if main_us not in us_mvp:
        print(f"[warning] US principale {main_us} introuvable dans backlog.csv (fiche {meta['uc']})")
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "—", "Phrase méthode agile": "—"})
    else:
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "O" if us_mvp[main_us] else "X", "Phrase méthode agile": us_phrase[main_us]})

print(pd.DataFrame(rows).to_markdown(index=False))
```

---

## Profil 5 : Intéressé public

**Description** — Citoyen, habitant ou journaliste souhaitant consulter la qualité de l'eau sur son territoire. Accès anonyme, interface simplifiée et filtrable, lecture seule stricte (aucun export, aucun partage).

**Objectifs** :

1. Vérifier la qualité de l'eau potable sur son lieu de résidence
2. Explorer la carte des captages et bassins versants de sa commune
3. Comprendre les indicateurs de qualité grâce à des données chiffrées
4. Accéder aux périmètres de protection et aux zones à risque
5. S'informer sur l'état de la ressource en eau sans outil technique

```{python}
#| label: tbl-public
#| tbl-cap: "Cas d'usage du profil Intéressé public (généré depuis backlog.csv)"
#| output: asis

import glob
import yaml
import pandas as pd

backlog = pd.read_csv("backlog.csv", sep=";")
backlog["MVP"] = backlog["MVP"].astype(str).str.strip().str.upper().eq("TRUE")
us_mvp = dict(zip(backlog["ID User Story"], backlog["MVP"]))
us_phrase = dict(zip(backlog["ID User Story"], backlog["Phrase méthode agile"]))

rows = []
for path in sorted(glob.glob("specifications/CU-*.qmd")):
    meta = yaml.safe_load(open(path, encoding="utf-8").read().split("---", 2)[1])
    if meta["profile"] != "Intéressé public":
        continue
    main_us = meta["us"].split(",")[0].strip()
    if main_us not in us_mvp:
        print(f"[warning] US principale {main_us} introuvable dans backlog.csv (fiche {meta['uc']})")
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "—", "Phrase méthode agile": "—"})
    else:
        rows.append({"#": meta["uc"], "Cas d'usage": CAS_USAGE[meta["uc"]], "US": meta["us"], "Question concrète": QUESTION[meta["uc"]], "MVP": "O" if us_mvp[main_us] else "X", "Phrase méthode agile": us_phrase[main_us]})

print(pd.DataFrame(rows).to_markdown(index=False))
```

---

## Matrice de couverture profil × périmètre

Nombre d'User Stories (US) du backlog par profil et par EPIC. « lecture seule » (Intéressé public) = aucun US dédié au profil sur ce périmètre (cases à 0), accès en consultation uniquement.

```{python}
#| label: tbl-matrice
#| tbl-cap: "Matrice de couverture profil × périmètre — nombre d'US par profil et par EPIC (générée depuis backlog.csv)"
#| output: asis

import pandas as pd

backlog = pd.read_csv("backlog.csv", sep=";")

epic_short = {
    "EPIC 1 – Gestion des données": "EP1",
    "EPIC 1bis – Catalogage des données": "EP1bis",
    "EPIC 2 – Qualité des données": "EP2",
    "EPIC 3 – Référentiels": "EP3",
    "EPIC 4 – Calcul d'indicateurs": "EP4",
    "EPIC 5 – Analyse multicritère": "EP5",
    "EPIC 6 – Visualisation": "EP6",
    "EPIC 7 – Aide à la décision": "EP7",
    "EPIC 8 – Export": "EP8",
    "EPIC 9 – Utilisateurs": "EP9",
    "EPIC 10 – Traçabilité": "EP10",
}

profil_order = [
    "Administrateur plateforme",
    "Administrateur expert data",
    "Expert métier eau potable",
    "Décideur métier",
    "Intéressé public",
]

mat = (
    pd.crosstab(backlog["Profil utilisateur"], backlog["EPIC"])
    .reindex(index=profil_order, columns=list(epic_short.keys()))
    .rename(columns=epic_short)
)
mat.index.name = "Profil \\ EPIC"

print(mat.to_markdown())
```

*Note : le profil « Intéressé public » est en lecture seule stricte, sans aucun export ni partage. Les US8.x d'export sont portées par l'Administrateur expert data (US8.1, US8.2, US8.4, US8.5, US8.6) et par le Décideur métier pour le rapport PDF (US8.3), conformément au backlog.csv. Matrice générée depuis backlog.csv (99 US).*
